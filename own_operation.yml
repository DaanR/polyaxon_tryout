version: 1.1
kind: operation
params:
  optimizer: {value: sgd}
  epochs: {value: 2}
runPatch:
  container:
    resources:
      limits:
        nvidia.com/gpu: 3
        cpu: 3
pathRef: own_component.yml